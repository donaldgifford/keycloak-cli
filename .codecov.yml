---
# yaml-language-server: $schema=https://www.schemastore.org/codecov.json
coverage:
  status:
    project:
      default:
        target: 60%
        threshold: 40%
ignore:
  - "cmd/keycloak-cli/main.go"
  - "docs"
  - "scripts"
